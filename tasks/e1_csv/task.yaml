id: e1_csv
category: feature
tier: mid
scoring: pytest
target_file: agg.py
prompt: |
  agg.py に関数 group_sum(rows, key, value) を追加してください。
  rows は辞書のリストです。key 列の値ごとに value 列の合計を求め、
  {キー: 合計} の辞書を返します。存在しない列名が渡されたら KeyError を送出してください。
budget:
  max_cost_usd: 2.00
  max_turns: 20
  max_wall_s: 400
